<% title = fmtMonthYear(year + '-' + month) + ' Transactions'; %>
<%- include('partials/head') %>
<p><a href="/years/<%= year %>/<%= month %>">← Back to Monthly Summary</a></p>
<h1><%= fmtMonthYear(year + '-' + month) %> Transactions</h1>
<%- include('partials/filterForm', {
  basePath: `/years/${year}/${month}/transactions`,
  yearsList,
  categoriesList: allCategories,
  year,
  month,
  category,
  dateFrom,
  dateTo,
  amountMin,
  amountMax
}) %>
<%- include('partials/bulkActionsForm', {
  basePath: `/years/${year}/${month}/transactions`,
  allCategories
}) %>
<%- include('partials/transactionList', {
  transactions,
  totalCount,
  totalPages,
  currentPage,
  allCategories,
  currency,
  basePath: `/years/${year}/${month}/transactions`,
}) %>
<%- include('partials/footer') %>
  <%- include('partials/categoryEditorScript') %>